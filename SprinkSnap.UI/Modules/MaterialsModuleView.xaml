<UserControl x:Class="FireSprinklerPlugin.SprinkSnap.UI.Modules.MaterialsModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Materials" FontSize="22" FontWeight="SemiBold" />
        <Button Grid.Row="1"
                Content="Refresh Takeoff"
                Command="{Binding RefreshCommand}"
                HorizontalAlignment="Left"
                Padding="14,8"
                Margin="0,12,0,12" />

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Items}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  MinHeight="240"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding ItemType}" Width="120" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*" />
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity, StringFormat=N0}" Width="100" />
                <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="80" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
